# Partner Logos

Place participating partner booth logos in this folder.

Recommended format:

- SVG for simple vector marks
- PNG or WebP for raster logos
- Use lowercase, hyphenated filenames such as `northstar-analytics.svg`

The current app uses generated initials as logo placeholders, so real assets can be wired into the booth mock data later without changing the localStorage layer.
